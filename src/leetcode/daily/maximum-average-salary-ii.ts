// TODO: Solve this problem
// Find subarrays that have length of at least k
// when the subarray length is at least k, then we should make sure
// that we keep track of the current sum of those elements and then average them
// this is probably why we want to keep a prefix sum
function findMaxAverage(nums: number[], k: number): number {
   // note that we can choose subarrays from lengths k to nums.length
   return 0;
}

export { findMaxAverage };
