// TODO: Solve this using a Trie
class FileSystem {
  constructor() {}

  createPath(path: string, value: number): boolean {
    return false;
  }

  get(path: string): number {
    return 0;
  }
}

export { FileSystem };
