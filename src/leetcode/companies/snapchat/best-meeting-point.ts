// TODO: Finish this problem later
function minTotalDistance(grid: number[][]): number {
   // given we have a binary matrix where we're trying to minimize the total distance
   // between all possible friends to a certain meeting place
   return -1;
}

export { minTotalDistance };
