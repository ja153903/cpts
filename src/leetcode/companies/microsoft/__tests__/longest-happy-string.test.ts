import { longestDiverseString } from '../longest-happy-string';

describe('Longest Happy String', () => {
   it('should pass some test', () => {
      longestDiverseString(1, 1, 3);
      longestDiverseString(1, 1, 7);
   });
});
