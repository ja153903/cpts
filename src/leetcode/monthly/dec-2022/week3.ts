function assignBikes(workers: number[][], bikes: number[][]): number {
  // for this problem, we might want to consider enumerating all possible pairings
  // of bike to worker
  // at each step we also maek down the running l1 distance

  return 0;
}

export { assignBikes };
