// TODO: This one is too hard; I don't get the structure of the problem
const MOD = Math.pow(10, 9) + 7;

function kInversePairs(n: number, k: number): number {
  let result = 0;

  return result % MOD;
}

/**
 * What is the brute force solution to this problem?
 * We can find all permutations of number and then check each permutation for the number of inverse pairs
 */

export { kInversePairs };
