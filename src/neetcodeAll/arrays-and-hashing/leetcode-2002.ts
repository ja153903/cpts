// TODO: Solve this at a later time.
function maxProduct(s: string): number {
   // this question feels like a dynamic programming question.
   // but let's try to get a brute force solution going first
   // The idea here would be to generate a palindrome keeping track
   // of the indices we've used for it.
   // Then given this one, we should also generate more palindromes
   // excluding the indices we've already used
   return 0;
}

export { maxProduct };
