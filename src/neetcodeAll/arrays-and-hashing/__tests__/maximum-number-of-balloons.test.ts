import { maxNumberOfBalloons } from '../maximum-number-of-balloons';

describe('Maximumm number of balloons', () => {
  it('should pass teh test', () => {
    expect(maxNumberOfBalloons('nlaebolko')).toBe(1);
  });
});
